# DOM
- El DOM es entonces una Web API del navegador que expone una representación del documento HTML como objetos que JavaScript puede manipular.
- El navegador interpreta el HTML y construye un DOM que conceptualmente es un árbol de nodos.

- El navegador interpreta el HTML y construye un DOM que conceptualmente es un árbol de - nodos.

## Agrgar clases al dom